cat > README.md <<'EOF'
# MONARCH-TOOLS

Python tools for customizing Monarch Money.

## Development
- Create venv: `python3.12 -m venv .venv`
- Install: `python -m pip install -U pip && python -m pip install -e .`
- Run tests: `pytest`
EOF